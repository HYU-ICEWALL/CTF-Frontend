import Auth from "../hoc/Auth.tsx";

function Problems() {
  // change to what I shared with lake later

  return (
    <>
      <Auth>
        <div>
          <p>문제 목록</p>
        </div>
      </Auth>
    </>
  );
}

export default Problems;
